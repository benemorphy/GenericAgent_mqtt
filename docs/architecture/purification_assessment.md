# 项目净化评估与规划

> 基于 `brainstorm_project_purification.md` + 2026-05-22 项目实际状态
> 日期: 2026-05-22

---

## 一、净化状态快照

| 脑暴报告的污垢 | 5月21日状态 | 当前状态 (5月22日) | 变动 |
|:---------------|:-----------|:------------------|:-----|
| `__pycache__` 11629个 | ❌ 严重 | ✅ 10个 | 自动清理 |
| `temp/` 临时文件 | 2.4MB | 20.9MB | 增加 (新脚本产出) |
| 根目录 29项 | ❌ 膨胀 | 24项 | 正在清理 |
| `skills_learning/` 被忽略 | ❌ | ❌ 仍是 | 未解决 |
| `tests/` 被忽略 | ❌ | ❌ 仍是 | 需改为显式忽略 |
| `.gitignore` 60+行 | ❌ 膨胀 | 181行 | ❌ 继续膨胀 |
| memory/ gitignore 规则 | 20+ | 31条 | ❌ 继续增加 |
| `mykey.py` 被追踪 | ❌ | ✅ 已不在git | 已修复 |
| `CLEAR=1/` | ❌ 存在 | ✅ 已删除 | 已修复 |

## 二、已完成的净化工作

| 事项 | 对应工作 | 说明 |
|------|---------|------|
| `__pycache__` 大清理 | 多次重建环境 | 11629→10 |
| `mykey.py` 取消追踪 | 工作流中自然修复 | 不再在git中 |
| `CLEAR=1/` 删除 | 工作流中自然修复 | 已不存在 |
| 根目录 29→24 | archive/ 迁移 + CLEAR=1 删除 | 减少5项 |

## 三、待处理的净化事项

### 第一阶段：紧急清理（零风险，立即执行）

| # | 事项 | 当前 | 操作 | 风险 |
|---|------|------|------|------|
| 1 | 清理 temp/ 过期脚本 | ~20.9MB | 删除 `temp/*.py`, `temp/tmp*` | 无 |
| 2 | 清理 `temp/model_responses/` >7天 | 未检查 | 保留最近3天 | 低 |
| 3 | 确认 `agents/`, `plugins/`, `profiles/` 等目录去留 | 仍存在 | 评估后合并到 `archive/` | 低 |
| 4 | L4_raw_sessions 保留策略 | 未自动清理 | 加入30天轮换 | 低 |

### 第二阶段：配置净化

| # | 事项 | 当前 | 操作 | 风险 |
|---|------|------|------|------|
| 5 | `.gitignore` 简化 | 181行, 31条memory规则 | `memory/*` → 白名单模式 | 中 |
| 6 | `tests/` 从 `.gitignore` 移除 | 被全局忽略 | 显式忽略 `__pycache__` | 低 |
| 7 | `skills_learning/` 追踪元数据 | 全局忽略 | 追踪 `review_tracker` 等 | 低 |

### 第三阶段：架构净化

| # | 事项 | 当前 | 操作 | 风险 |
|---|------|------|------|------|
| 8 | 评估 agents/plugins/profiles/reflect/ 等目录 | 24根目录项 | 可合并非活跃目录 | 中 |
| 9 | temp/ 自动清理 | 无 | 集成到 `session_compactor` | 低 |

## 四、优先级建议

| 优先级 | 事项 | 预估工时 | 理由 |
|--------|------|---------|------|
| **P0** | `.gitignore` 简化 (规则181→40) | 0.5h | 当前模式不可持续 |
| **P0** | temp/ 过期清理 schema (20MB→5MB) | 0.3h | 磁盘膨胀 |
| **P1** | tests/ 从 .gitignore 移除 | 0.2h | CI 混淆风险 |
| **P1** | skills_learning/ 选择性追踪 | 0.5h | 反刍产物丢失 |
| **P2** | 根目录瘦身 (24→18) | 1h | 架构整洁 |
| **P2** | L4_raw_sessions 自动轮换 | 0.5h | 安全合规 |

## 五、建议推进路径

```
当前 → P0: .gitignore 简化 + temp/ 清理 → P1: tests + skills_learning → P2: 根目录
         (立即)                      (下轮)                   (后续)
```
